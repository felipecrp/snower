import { Component } from '@angular/core';

/** Export page — download results in various formats. */
@Component({
  selector: 'app-export',
  templateUrl: './export.html',
})
export class ExportPage {}
