import { Component } from '@angular/core';

/** Report page — summary and statistics for the current review. */
@Component({
  selector: 'app-report',
  templateUrl: './report.html',
})
export class ReportPage {}
