import { Component } from '@angular/core';

/** Analysis page — explore patterns and metrics across the paper set. */
@Component({
  selector: 'app-analysis',
  templateUrl: './analysis.html',
})
export class AnalysisPage {}
